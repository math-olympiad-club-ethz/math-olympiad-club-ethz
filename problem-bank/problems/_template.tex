% title:          Example problem title
% area:           polynomial-roots, complex-analysis
% methods:        substitution, rouche-theorem
% difficulty:
% difficulty-ai:  hard
% status:
% review:         none
% --- history: all optional, leave EMPTY if unknown ---
% origin:         putnam
% origin-date:    1989
% origin-number:  A3
% also-in:
% taken-from:
% references:
% history:        none
%
% Header rules (see README "Add a problem"):
%   title          short (<= 6 words, plain text); blank = "Untitled problem".  Name a NEW file
%                  new-<title-in-kebab-case>.tex: it is renamed NNNN-<title>.tex automatically after the merge
%   area           >= 1 slug from tags.yml (any tree level), comma-separated
%   methods        optional slugs from tags.yml; hidden by default on the site (spoilers)
%   difficulty     HUMAN rating: easy | medium | hard | extreme (leave empty until rated)
%   difficulty-ai  AI suggestion, same scale
%   status         "partial" only if the solution below is unfinished; otherwise leave empty
%   review         none | ai | human  — only "human" is published on the site
%   origin         source slug of the FIRST known appearance (tags.yml origins)
%   origin-date    YYYY | YYYY-MM | YYYY-MM-DD ;  origin-number  e.g. A3, P6, 12.1
%   also-in        other source slugs, comma-separated
%   taken-from     where the club got it (free text)
%   references     free text / URLs
%   history        none | ai | human — has the history above been checked?
% Everything except one area tag and the problem block is optional.
% \cite{key}: key in references.bib, or in an optional block at the very end of the file (this problem only):
%   \begin{bibentries}
%   @article{key, author = {...}, title = {...}, journal = {...}, year = {...}}
%   \end{bibentries}
% Delete these explanatory comment lines in a real problem file.
\begin{problem}
STATEMENT (paste the problem statement here).
\end{problem}
\begin{solution}
SOLUTION (optional; delete this block if there is no solution yet).
\end{solution}
