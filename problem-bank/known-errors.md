# Known errors in the problems

Mistakes found in the statements and solutions of `problems/` that need a mathematical fix. The pure typos were fixed
on 2026-09-23 (with Antoine's OK). Each remaining entry has a **Proposal** written by an AI: check it before applying.
Fix the file, then delete its line here. Delete this file when it is empty.
Line numbers in the entries refer to the old 2025 files (now removed): search the problem file for the quoted text.

## Errors in statements and solutions (84 left)

- **0002** — (a) The case where Q_1 = (1,1,1,1) although Q_0 is not (e.g. Q_0 = (1/b, b, 1/b, b)) is not treated: the argument starts with 'if at least one number in Q_1 is not 1'. The missing case is trivial (the sequence is then constant and never returns to Q_0) but should be stated.
  - **Proposal:** Add, right after 'if at least one number in Q_1 is not 1': 'If instead $Q_1=(1,1,1,1)$ (which can happen even when $Q_0\neq(1,1,1,1)$, e.g. $Q_0=(1/b,b,1/b,b)$), the sequence is constant equal to $(1,1,1,1)$ from then on, so it never returns to $Q_0$ unless $Q_0=(1,1,1,1)$ itself (excluded by hypothesis).' This closes the gap without touching the rest of the argument.
- **0002** — (b) The formula for the first entry after r steps needs the indices a_{i+1} taken cyclically modulo 2^k; this is not said. The parity of binom(2^k, i) for 0 < i < 2^k is only hinted at ('the highest power of 2 dividing n is greater than that dividing i or n-i'); the actual deduction (e.g. binom(n,i) = (n/i) binom(n-1,i-1), or Kummer/Lucas) is not written out.
  - **Proposal:** Two additions needed in part (b): (1) state explicitly that indices are taken mod $2^k$, i.e. $a_{i+1}$ means $a_{((i \bmod n)+1)}$ so the product $\prod_{i=1}^{r} a_{i+1}^{\binom{r}{i}}$ wraps around; (2) write out the parity argument for $\binom{n}{i}$, $n=2^k$, $0<i<n$: by Kummer's theorem the exponent of 2 in $\binom{n}{i}$ equals the number of carries when adding $i$ and $n-i$ in base 2, which is at least 1 because $i$ and $n-i$ both have fewer than $k$ bits (since $0<i<2^k$) so a carry must occur to reach the single bit $2^k$; hence $\binom{n}{i}\equiv 0 \pmod 2$ for all $0<i<n$.
- **0007** — Solution, display 'a_n = prod_{i=N}^{n-1} a_{i+1}/a_i < 1/(a_N 2^{n-1-N}) = (a_N^{-1} 2^N + 1)/2^n': the product telescopes to a_n/a_N (the factor a_N is missing) and the last equality is false (1/(a_N 2^{n-1-N}) equals a_N^{-1} 2^{N+1}/2^n). The conclusion n a_n -> 0 still holds since a_n <= C 2^{-n}.
  - **Proposal:** Replace 'a_n = \prod_{i=N}^{n-1} \frac{a_{i+1}}{a_i} < \frac{1}{a_N 2^{n-1-N}} = \frac{a_N^{-1} 2^N + 1}{2^n}' with: '$a_n = a_N \prod_{i=N}^{n-1} \frac{a_{i+1}}{a_i} < a_N\cdot\frac{a_{N+1}}{a_N}\cdot\left(\frac12\right)^{n-1-N} = a_{N+1}\,2^{N+1-n} = \frac{a_{N+1}\,2^{N+1}}{2^n}$' (telescoping identity is $a_n/a_N=\prod$, the $i=N$ ratio is left as $a_{N+1}/a_N$, and only the ratios for $i>N$ are bounded by $1/2$). Correspondingly the next display 'n a_n < (a_N^{-1}2^N+1)(n/2^n)' should read '$n a_n < a_{N+1}2^{N+1}\cdot \frac{n}{2^n}$'. The conclusion $na_n\to0$ is unaffected.
- **0008** — Arithmetic slip (legacy solutions line 41): with s = -2+k one gets s^2-4s-12 = k^2-8k = k(k-8), not 'k^2-8k-16 = (k-4)^2' (and (k-4)^2 = k^2-8k+16 anyway). The conclusion (>= 0 for s <= -2, i.e. k <= 0) remains true.
  - **Proposal:** Replace '(k-2)^2-4(k-2)-12=k^2-8k-16=(k-4)^2' with '(k-2)^2-4(k-2)-12=k^2-8k=k(k-8)', and justify $\geq 0$ directly by noting that $s\le -2 \iff k\le 0$, so $k\le 0$ and $k-8<0$, hence $k(k-8)\ge 0$ (product of two non-positive numbers). The conclusion (holds for $s\le -2$) is unchanged.
- **0008** — Minor: 's >= -2 implies (a+b)^2/(ab) > 0' should be '>= 0'; the argument needs s > -2 and t > -2 strictly (the case s = -2 is already covered by the first case).
  - Typo part already fixed: '(a+b)^2/ab>0' -> '(a+b)^2/ab\geqslant 0' (matches what s\geqslant -2 actually gives)
  - **Proposal:** The step 'so $ab>0$' still needs justification: $\frac{(a+b)^2}{ab}\geqslant 0$ alone allows the degenerate case $ab<0$ with $a+b=0$ (i.e. $s=-2$ exactly). Since $s=-2$ (and likewise $t=-2$) is already covered by the first case ($s\le -2$), this branch should assume $s>-2$ and $t>-2$ strictly; then $\frac{(a+b)^2}{ab}=s+2>0$ forces $ab>0$ rigorously (and similarly $bc>0$ from $t>-2$).
- **0009** — The exponent bookkeeping 'k - 1 + sum a_i = n' comes from phi(m) = 2^(k-1) prod(p_i - 1), which is only valid for k >= 1 (m even); when m is odd (k = 0) the correct relation is sum a_i = n. This affects the prime case of a = 2 (n odd) and the a = 3 bounds with k = 0; the conclusions still hold with the corrected relation, but the written derivation is not right as stated.
  - **Proposal:** State the relation $k-1+\sum a_i=n$ only for the case $m$ even ($k\ge1$); add that when $m$ is odd ($k=0$), $\varphi(m)=\prod(p_i-1)=\prod 2^{a_i}=2^{\sum a_i}$, so the correct relation is $\sum_{i=1}^r a_i=n$ (i.e. the '$k-1$' term is simply absent, not $-1$). Re-check that the subsequent case analysis ($a=2$, $n$ odd $\Rightarrow$ $m=2^n+n$ odd $\Rightarrow k=0$; and the $a=3$, $k\in\{0,1,2\}$ bookkeeping) is re-derived with this corrected relation; the stated conclusions appear to still hold but the derivation as written should use $\sum a_i=n$ when $k=0$.
- **0009** — Justification of x(x-1) < 2^x via 'x^2 < 2^x since ln(x) < x/2' is wrong for 2 < x < 4 (e.g. 3^2 = 9 > 8) and ln(x) < x/2 gives x^2 < e^x, not 2^x. The inequality x(x-1) < 2^x needed for f(x) = x - sqrt(2^x + x) < 0 is nevertheless true for all x >= 0.
  - **Proposal:** Replace the justification 'since $\ln(x)<x/2$' (which gives $x^2<e^x$, not $x^2<2^x$, and is false as an argument for $2<x<4$) with a direct check: $f(x)=x-\sqrt{2^x+x}$ is negative iff $x^2 < 2^x+x$ (for $x\ge0$) iff $x(x-1)<2^x$. Verify directly for $0\le x\le 4$ (finitely many/continuity check, e.g. at $x=4$: $12<16$), and for $x>4$ prove $x(x-1)<2^x$ by induction/convexity (e.g. $2^x$ grows faster than any quadratic beyond $x=4$, via $2^{x+1}/2^x=2$ vs $(x+1)x/(x(x-1))\to1$).
- **0009** — The final intersection is written '{85} cap {...}' although {3^j + j : j >= 4} is infinite; it is only because every element of the second set is <= 100 that j = 4 is the sole candidate. Correct in effect, sloppy as written.
  - **Proposal:** Before writing '={85}\cap{...}', add: 'since every element of the right-hand set is $\le 100$ and $j\mapsto 3^j+j$ is increasing with $3^5+5=248>100$, only $j=4$ (giving $3^4+4=85$) can possibly lie in the intersection'; then the displayed equality follows.
- **0011** — Minor rigour gap: tr(A) and tr(A^2) are written as sums over the spectrum weighted by geometric multiplicities dim ker(A - lambda I); this equals the trace only because A is diagonalisable (its minimal polynomial divides P, which has simple roots), which is not stated. Likewise the tr(A^2) formula would double-count if two eigenvalues had the same square (not the case here).
  - **Proposal:** After introducing $P(X)=X(X-1)(X^2+X-1)$, add: 'Since $P$ has four simple real roots, the minimal polynomial of $A$ (which divides $P$) also has simple roots, so $A$ is diagonalisable over $\mathbb{R}$; hence $\operatorname{tr}(A)=\sum_\lambda \dim\ker(A-\lambda I)\cdot\lambda$ and likewise for $A^2$ (whose eigenvalues $\lambda^2$ are pairwise distinct since $0,1,\left(\tfrac{-1\pm\sqrt5}{2}\right)^2$ are four distinct values, so no double-counting occurs).'
- **0012** — Dangling cross-reference: the solution says the eigenvalues of A^2 are lambda_i^2 'for the same reason as the previous problem' -- this points to the footnote in legacy Linear Algebra 2 (now 0011), which is no longer adjacent once the problems are split.
  - **Proposal:** Replace 'for the same reason as the previous problem' with an inline restatement, e.g.: 'because $A$ is diagonalisable (it is symmetric), so writing $A=PDP^{-1}$ with $D=\operatorname{diag}(\lambda_1,\dots,\lambda_n)$ gives $A^2=PD^2P^{-1}$, i.e. $A^2$ has eigenvalues $\lambda_1^2,\dots,\lambda_n^2$.'
- **0014** — Statement uses the unusual notation s(n) = sum_{N ni d \|_Z n} d for the divisor sum (compiles, but odd).
  - **Proposal:** Optional clarity rewrite only (current notation is valid, not incorrect): replace '$s(n)=\sum_{\mathbb{N}\ni d|_{\mathbb{Z}}n}d$' with the more standard '$s(n)=\sum_{d\mid n} d$' (sum over the positive divisors $d$ of $n$). Left untouched since the entry does not flag it as wrong and gives no exact required replacement.
- **0020** — Results listing: the 'Exponential (t=1)' improvement (-0.18) is the theoretical gain minus the simulated naive gain, mixing a theoretical and an empirical quantity; harmless but confusing.
  - **Proposal:** In the code, `improvement = theo - naive_gain` mixes the theoretical expected gain of the g-strategy with the *simulated* naive gain, not a theoretical one. To compare like with like: either introduce a theoretical naive gain (the constant `(k+1)/2*alpha`) and use `improvement = theo - (k+1)/2*alpha`, or compare only empirical quantities via `improvement = exp_gain - naive_gain`. This only affects the illustrative Python printout, not the mathematics of the solution.
- **0021** — False general claim: 'The minimal polynomial is irreducible over Q[X]' -- the minimal polynomial of a matrix is not irreducible in general (e.g. diag(1,2)). The intended (and valid) argument: P_min,B divides (X^4+1)^2 with X^4+1 irreducible, and deg P_min,B <= 4 since B is 4x4, so P_min,B = X^4+1.
  - **Proposal:** Replace: "The minimal polynomial is irreducible over $\mathbb{Q}[X]$ and divides all polynomials with rational coefficient that vanish at \( B \); in particular, \( P_{\text{min},B}(X) \) must be a divisor of the polynomial \( P_{\text{char},A}\left(X^2\right) = \left(X^4 + 1\right)^2 \). So \( P_{\text{min},B}(X) \) must be a divisor of the polynomial $X^4 + 1$." (the claim that the minimal polynomial is irreducible is false in general, e.g. diag(1,2)) with: "The minimal polynomial $P_{\text{min},B}(X)$ divides every polynomial with rational coefficients that vanishes at $B$; in particular, it must divide $P_{\text{char},A}(X^2) = (X^4+1)^2$. Since $X^4+1$ is irreducible over $\mathbb{Q}[X]$ (shown below), the only monic divisors of $(X^4+1)^2$ are $1$, $X^4+1$, and $(X^4+1)^2$. As $B$ is a $4\times4$ matrix, $\deg P_{\text{min},B}\le 4$, ruling out $(X^4+1)^2$ (degree $8$); and $P_{\text{min},B}\neq 1$ since a minimal polynomial has degree $\ge 1$. Hence $P_{\text{min},B}(X) = X^4+1$." (keep the following cyclotomic-polynomial irreducibility argument as is, just move/reuse it to justify "irreducible over Q[X]" above).
- **0023** — duplicate \label{1} in the legacy algebra solutions (0023 and 0031; harmless once split) (maintainer)
  - **Proposal:** No action needed: this and E36 describe a \label{1} clash between the old, merged algebra_solutions.tex (0023 and 0031 sharing one file). Now that each problem has its own file, 0023 contains \label{1} exactly once (line 27) — there is nothing to fix inside this file.
- **0023** — Duplicate '\label{1}' with the SFFT solution (0031) in the legacy algebra_solutions.tex (lines 61 and 173); harmless once split, reported as requested.
  - **Proposal:** Same clash as E35 (0023 vs 0031's SFFT solution), already resolved by the split into per-problem files; no edit needed here.
- **0023** — A '\footnote{...}' is placed inside display math (legacy solutions line 88, inside '\[ ... \]'); LaTeX accepts it here but footnotes in display math are fragile (\footnotemark/\footnotetext would be the safe form).
  - **Proposal:** If robustness is wanted: in "\left\langle G(m)\cup G(n) \right\rangle=\left\langle\left\{g^m \mid g\in G\right\}\cup\left\{h^n \mid h\in G\right\}\right\rangle\footnote{...}." (inside a \[ ... \] block), replace \footnote{...} with \footnotemark right after \right\rangle, and move its content to \footnotetext{...} placed immediately after the closing \]. Purely cosmetic robustness; the file compiles fine as is, so left unchanged.
- **0023** — Equation (1) uses von Neumann ordinal notation ('\prod_{i \in k}', 'k = \{i \in \mathbb{N} \mid i < k\}'), unusual but explained in the text.
  - **Proposal:** No error: the von Neumann ordinal notation ($\prod_{i\in k}$, $k=\{i\in\mathbb{N}\mid i<k\}$) is unusual but is defined in the same paragraph (equation (1) and the line just above it). No change needed.
- **0025** — The statement uses numbered \subsection{...} headings inside the problem environment; in a standalone problem file they will print as '0.1 Problem 6 (IMO 1988)' and '0.2 Problem (Kevin Buzzard & Edward Crane)'. Cosmetic, not fixed.
  - **Proposal:** If wanted: change \subsection{Problem 6 (IMO 1988)} and \subsection{Problem (Kevin Buzzard \& Edward Crane)} (inside \begin{problem}) to the starred \subsection*{...} form, matching \subsection*{Solution:} already used later, so the headings don't print as numbered '0.1 ...' / '0.2 ...'. Left as a cosmetic call for the maintainer, per the known-errors note ('cosmetic, not fixed').
- **0027** — Solution 1, last sentence: the decomposition 'N_{>0} = disjoint union over N in 3N of [[N+1; 3N]]' is wrong as written (multiples of 3 give overlapping blocks); the intended index set is the powers of 3 (N = 3^k, k >= 0), and the integer 1 is not covered by these blocks (harmless).
  - **Proposal:** Replace "The result follows directly because we have the infinite decomposition \(\mathbb{N}_{>0}= \bigsqcup_{N\in 3\mathbb{N}} \llbracket N+1;3N \rrbracket\)." with: "The result follows because, apart from the single harmless term $n=1$, we have the infinite decomposition $\mathbb{N}_{>0}\setminus\{1\} = \bigsqcup_{k \ge 0} \llbracket 3^k+1;\,3^{k+1} \rrbracket$ (taking $N=3^k$ for $k\ge0$; blocks $\llbracket N+1;3N\rrbracket$ for general $N\in3\mathbb{N}$ overlap), each contributing more than $1/9$ to the sum."
- **0030** — Two \footnote commands sit inside display math (statement footnote on \pmod{m}, and the Legendre-formula footnote after 'v_p(i!) ='); LaTeX tolerates them but the mark/text placement is fragile.
  - **Proposal:** If robustness is wanted: for the two footnotes sitting inside display math (the \pmod{m} footnote in the problem statement, and the Legendre's-formula footnote right after 'v_p(i!) ='), replace each \footnote{...} with \footnotemark at that spot and move the note text to a \footnotetext{...} placed right after the closing \]/\). Both compile fine as-is, so left unchanged.
- **0031** — AIME 1998 solution is unfinished: it stops at 'p, (m-2)(n-2)-8 > 0' without determining the largest p.
  - **Proposal:** Continue after "we have $p,(m-2)(n-2)-8>0$": since $m=1,2$ make $(m-2)(n-2)-8\le -8<0$, impossible, so $m\ge3$. For fixed $m$, $p=\dfrac{2(m+2)(n+2)}{(m-2)(n-2)-8}$ is maximized by making the (positive) denominator as small as possible. For $m=3$: denominator $=n-10$, minimal value $1$ at $n=11$, giving $p=\dfrac{2\cdot5\cdot13}{1}=130$ (check: $3\cdot11\cdot130=4290=\tfrac12\cdot5\cdot13\cdot132$, and $3\le11\le130$). For $m\ge4$ the same optimization gives strictly smaller $p$ (e.g. $m=4$ gives at best $p=54$, at $n=7$). Hence the largest possible value of $p$ is $\boxed{130}$.
- **0031** — BMO 2005 solution block is empty (\solution[Solution:] followed by nothing).
  - **Proposal:** Fill the empty \subsection*{Solution:} for BMO 2005 with: From $1/x+1/y=1/N$ one gets $(x-N)(y-N)=N^2$, so $(x,y)\mapsto x-N$ is a bijection between the solution pairs and the positive divisors of $N^2$; hence $d(N^2)=2005=5\cdot401$ ($401$ prime). Writing $N=\prod p_i^{a_i}$, $d(N^2)=\prod(2a_i+1)=2005$. The only divisors of $2005$ are $1,5,401,2005$, all $\equiv1\pmod4$, so every factor $2a_i+1$ occurring is $\equiv1\pmod4$, forcing $a_i=\frac{(2a_i+1)-1}{2}$ to be even. Hence every exponent in $N$ is even, i.e. $N$ is a perfect square.
- **0031** — Duplicate '\label{1}' with the IMC 2005 solution (0023) in the legacy algebra_solutions.tex; harmless once split.
  - **Proposal:** No action needed: duplicate \label{1} was between 0023 and 0031 in the old merged algebra_solutions.tex. 0031 now has \label{1} exactly once (line 58); resolved by the split, nothing to fix here.
- **0031** — Cosmetic: the problems file uses numbered \subsection{...} for the five sub-problems (prints as 0.1, 0.2, ...) while the solutions file uses \subsection* for the first one only.
  - **Proposal:** Cosmetic, matching E41: if wanted, switch the five \subsection{...} sub-problem headings inside \begin{problem} to \subsection*{...}, matching the \subsection*{Solution:} headings already used for problems 2-5 in the solution, so they don't print as numbered '0.1 AMC 12 (2012)', etc. Left as a maintainer call, per the known-errors note.
- **0033** — Statement: the polynomial sum_{i=0}^{n} c_i T^i has n+1 coefficients but the family is written (c_i)_{i in n} in Q^n (off by one); same in the solution l. 352. The indeterminate is T in the sum but the ring is written Q[X].
  - **Proposal:** Off-by-one + notation: statement (line 21) writes 'the rational coefficients $(c_i)_{i \in n} \in \mathbb{Q}^n$' but $\sum_{i=0}^n c_iT^i$ has $n+1$ coefficients; should be $(c_i)_{i \in n+1} \in \mathbb{Q}^{n+1}$. Same fix needed in the solution (line 106): '$\mathbf{c} := (c_i)_{i \in n} \in \mathbb{Q}^n$' -> '$\mathbf{c} := (c_i)_{i \in n+1} \in \mathbb{Q}^{n+1}$'. Also on line 17 the sum uses indeterminate $T$ but is declared '$\in\mathbb{Q}[X]$'; should be '$\in\mathbb{Q}[T]$'.
- **0033** — Solution l. 274-276: leftover author's note 'This solution needs to be written more formally' typeset as a displayed substack.
  - **Proposal:** Lines 28-30: leftover '\substack{\textit{This solution needs to be written more formally}}' display note, even though a full argument follows. Whether to delete it, keep it as an honest caveat, or reformat it without the display-math hack is a content call for Antoine, not a mechanical fix.
- **0033** — Solution l. 328-332: f_1((a_0,...,a_{k-1})) = prod p_i^{a_i+1} - 1 is injective but NOT surjective onto N (e.g. 2 = 3 - 1 is never attained because the exponent of p_0 = 2 is always >= 1), so it is not a bijection N^{<omega} -> N as claimed.
  - **Proposal:** $f_1$ (lines 82-86, prime encoding) is injective but not surjective onto $\mathbb{N}$ (e.g. 2 is never hit), so the heading 'The prime encoding bijection:' (line 82) and the later 'constructive bijections $g_1,g_2,g_3$' (line 104) are wrong for $g_1$. Suggested minimal fix: rename the heading to 'The prime encoding injection:' and adjust line 104 as proposed under E57.
- **0033** — Solution l. 334-338: f_2((a_0,...,a_{k-1})) = sum_j binom(a_j + j, j+1) is not injective (f_2((3)) = 3 = f_2((3,0))), so it is not a bijection either. Only f_3 (recursive Cantor pairing) is a genuine bijection; hence 'constructive bijections g_1, g_2, g_3' (l. 350) is wrong for g_1, g_2. The strategy only needs a surjection N -> Q^{<omega}, which g_3 provides.
  - **Proposal:** $f_2$ (lines 88-92, multiset rank) is not injective ($f_2((3))=f_2((3,0))=3$), so it is not a bijection either; only $f_3$ (recursive Cantor pairing) is a genuine bijection $\mathbb{N}\to\mathbb{N}^{<\omega}$, and only a surjection is actually needed. Suggested replacement for line 104 'These induce constructive bijections $g_1, g_2, g_3: \mathbb{N} \to \mathbb{Q}^{<\omega}$.': 'Composing with the bijection $\mathbb{N}\simeq\mathbb{Q}$, $f_3$ induces a constructive bijection $g_3:\mathbb{N}\to\mathbb{Q}^{<\omega}$ ($f_1$ is injective but not surjective, and $f_2$ is neither; the strategy only needs a surjection $\mathbb{N}\to\mathbb{Q}^{<\omega}$, which $g_3$ already supplies).' Also rename the $f_2$ heading (line 88) from 'bijection' to 'map'.
- **0034** — Solution, summary: 'A cap (Z x Z) = Delta(Z_{>0}) disjoint-union {(2,4),(4,2)}' should be Delta(Z_{>=0}): (0,0) belongs to A with the convention 0^0 = 1 stated in the footnote, and the earlier line correctly writes Delta(Z_{>=0}).
  - **Proposal:** Line 220: '$A\cap(\mathbb{Z} \times \mathbb{Z}) = \Delta(\mathbb{Z}_{>0}) \sqcup \{(2,4), (4,2)\}$' should read $\Delta(\mathbb{Z}_{\geq 0})$ instead of $\Delta(\mathbb{Z}_{>0})$, since $(0,0)\in A$ under the stated convention $0^0=1$, matching $\Delta(\mathbb{Z}_{\ge0})$ used two lines above (line 100).
- **0034** — Bonus solution: with t = y/x one has u = 1/(t-1) = x/(y-x) and v = t/(t-1) = y/(y-x), not 'u = y/(x-y)' and 'v = x/(x-y)'; likewise t = u^{-1} + 1 and t = (v-1)^{-1} + 1, not '(u-1)^{-1} + 1' and 'v^{-1} + 1'. These expressions are not used afterwards, the argument stands.
  - **Proposal:** Line 263: '$u := \frac{1}{t - 1} = \frac{y}{x - y} \neq 0$ and $v := \frac{t}{t - 1} = \frac{x}{x - y} \neq 0,1$' should read '$u := \frac{1}{t-1} = \frac{x}{y-x} \neq 0$ and $v := \frac{t}{t-1} = \frac{y}{y-x} \neq 0,1$' (with $t=y/x$). Later on the same line, '$t = (u - 1)^{-1} + 1$ or $t = v^{-1} + 1$' should read '$t = u^{-1} + 1$ or $t = (v-1)^{-1} + 1$'. Neither expression is reused, so the rest of the argument stands.
- **0034** — Bonus solution, last display: the left-hand side reads 'A cap (Q^alg x Q^alg)' but what is summarised is the algebraic-integer intersection A cap (O x O) (copy-paste error).
  - **Proposal:** Line 294: LHS of the final display, '$A \cap ( \mathbb{Q}^{\mathrm{alg}} \times \mathbb{Q}^{\mathrm{alg}} ) = \Delta( \mathcal{O}_{\mathbb{Q}^{\mathrm{alg}}}(\mathbb{Z}) \cap \mathbb{R}_{\geq 0} ) \sqcup \{\dots\}$' should read '$A \cap ( \mathcal{O}_{\mathbb{Q}^{\mathrm{alg}}}(\mathbb{Z}) \times \mathcal{O}_{\mathbb{Q}^{\mathrm{alg}}}(\mathbb{Z}) ) = \dots$' (copy-pasted intersection on the LHS; the RHS already describes the algebraic-integer case).
- **0034** — Appendix: the Gelfond-Schneider theorem is stated with 'Proof not yet written'.
  - **Proposal:** Lines 304-309: the Gelfond-Schneider theorem is stated in the appendix with '\substack{\textit{Proof not yet written}}' — no proof at all is given. This needs an actual proof, or an explicit citation in place of an appendix 'theorem'; not something to fabricate here.
- **0035** — Off-by-one in the coefficient formula for C_n(X) = A_n(X) B_n(X): the pairing sum is written with upper index floor(k/2) - 1 for every k, which is empty for k = 1 and misses the pair (j, k-j) = (floor(k/2), ceil(k/2)) whenever k is odd; the upper index should be floor(k/2) for odd k and floor(k/2) - 1 for even k. The conclusion c_k in Z is unaffected.
  - **Proposal:** Line 85: the coefficient formula's sum upper index $\left\lfloor k/2 \right\rfloor - 1$ is wrong (empty for k=1, misses a pair for odd k); replace both occurrences of that upper limit by $\left\lceil k/2 \right\rceil - 1$ (the $\lfloor k/2\rfloor$ inside the indicator/middle term stays as is). $c_k\in\mathbb{Z}$ still holds.
- **0035** — Notation drifts between Gamma_{A_n,+} / Gamma_{B_n,-} (definition) and Gamma_{A,n} / Gamma_{B,n} (step 1).
  - **Proposal:** Line 109 uses $\Gamma_{A,n}$ / $\Gamma_{B,n}$ where the definitions (lines 43-44) call them $\Gamma_{A_n,+}$ / $\Gamma_{B_n,-}$. Suggest replacing the four occurrences on line 109 with $\Gamma_{A_n,+}$ / $\Gamma_{B_n,-}$ to match the rest of the solution.
- **0037** — Notational slip in the final verification (legacy solutions lines 547-555): 'f^{m}(Orb_eval(s))' etc. apply f^m to the orbit (a set) instead of to its chosen representative chi(Orb_eval(s)) as in the definitions of h-hat and k-hat just above.
  - **Proposal:** Lines 124-132 (final verification): every $f^{\bullet}(\operatorname{Orb}_{\operatorname{eval}}(s))$ should be $f^{\bullet}(\chi(\operatorname{Orb}_{\operatorname{eval}}(s)))$, matching the definitions of $\hat h,\hat k$ above. Wrap $\operatorname{Orb}_{\operatorname{eval}}(s)$ in $\chi(\cdot)$ in: the opening premise 'there exists m with $f^m(\operatorname{Orb}_{\operatorname{eval}}(s))=s$', and in the three displayed computations of $\hat h\circ\hat h(s)$, $\hat k\circ\hat k(s)$, $\hat h\circ\hat k(s)$.
- **0038** — Statement (both files): 'the intersection point of the common tangent to C_i and C_j' is ambiguous — two disjoint circles have four common tangents; the intended point is the intersection of the two EXTERNAL common tangents (external homothety centre). With the internal tangents the statement is false in general.
  - **Proposal:** Line 17: 'let $T_{ij}$ be the intersection point of the common tangent to $C_i$ and $C_j$' should read '...the intersection point of the two external common tangents to $C_i$ and $C_j$ (their external homothety centre)...' — disjoint circles of different radii have 4 common tangents, and collinearity fails in general for internal tangents.
- **0038** — Solution sketch (geometry_solutions.tex line 19): 'each line T_{ij} (the line determined by the common tangent point in the plane)' — T_{ij} is a point, not a line; the lifted objects are the lines X_i X_j through the apexes, and the argument that they pass through T_{ij} (similar triangles in the vertical plane through the two centres) is only asserted, not carried out.
  - **Proposal:** Line 25: 'each line $T_{ij}$ (the line determined by the common tangent point in the plane)' is wrong — $T_{ij}$ is a point, not a line; the lifted objects are the lines $X_iX_j$ through the apexes. Needs to say $X_iX_j$ meets $\pi$ at $T_{ij}$, and the similar-triangles argument for that is currently only asserted, not carried out.
- **0038** — The placeholder sentence 'This solution needs to be written more formally' is typeset inside display math with \substack{\textit{...}} (compiles thanks to amsmath, but it is a hack).
  - **Proposal:** Lines 20-22: same placeholder as 0033/E55, typeset via \substack{...} inside display math just to get italic text; compiles but is a hack. Whether to reformat or remove it (the sketch is admittedly incomplete anyway, cf. E68) is Antoine's call.
- **0039** — Solution is only a heuristic sketch (states the pairing idea 'for every invisible point there is a matching visible point' without constructing the pairing); the legacy text itself says it needs to be written formally.
  - **Proposal:** The solution (lines 20-28) never constructs the claimed pairing between invisible and visible points; it only asserts one exists. An explicit bijection/pairing between the shadowed region of each sphere and a region made visible elsewhere, with justification that areas match, is missing mathematical content — not something to draft here.
- **0040** — Solution block is a placeholder: no proof, only a link to https://math.stackexchange.com/questions/969781 ; the sentence 'This solution needs to be written more formally' is typeset inside display math with \substack{\textit{...}} (compiles thanks to amsmath, but it is a hack).
  - **Proposal:** The whole solution (lines 24-27) is just a link to a Math.SE post; there is no written proof in the file. An actual solution needs to be written out (e.g. the area-under-staircase / Sean Lo argument referenced), not just an external pointer.
- **0041** — polynomials_solutions.tex lines 286-289 (Solution 2): the factor (-1)^{2^n} is silently dropped when passing to 2^{2^{n+1}} P(-1/4); this is valid for n >= 1 only (for n = 0 the polynomial is X^2+X+1 and the claim is trivial), while Solution 1 fixes an arbitrary n in N.
  - **Proposal:** Lines 146-150: passing from $\prod(-4(\dots)) = 2^{2^{n+1}}(-1)^{2^n}\prod(\dots)$ to '=$2^{2^{n+1}}P(-1/4)$' silently uses $(-1)^{2^n}=1$, true only for $n\ge1$ ($2^0=1$ is odd). Suggest adding right after that display: '(here $(-1)^{2^n}=1$ since $2^n$ is even for $n\ge1$; for $n=0$, $P(g(X))=X^2+X+1$ is irreducible directly).'
- **0041** — polynomials_solutions.tex: \tag{1} is used three times (labels 4.1, A.1.1, A.2.1), so equation numbers (1) are displayed several times; harmless for the labels, only the printed numbering is ambiguous.
  - **Proposal:** Three equations (labels 4.1, A.1.1, A.2.1) are each given \tag{1}, so '(1)' prints three times, though \label/\eqref still resolve correctly. Needs a renumbering decision (e.g. drop the manual \tag overrides and let LaTeX number continuously, or tag by subsection like \tag{A.1.1}) touching several equations across the solution and both appendix subsections — not a one-line fix.
- **0044** — Part 2: 'x_{2n+1} = x_{2n-1}/3 + o(x_{2n-1}^3)' should read x_{2n+1} = x_{2n-1}^3/3 + o(x_{2n-1}^3) (u - sin u + sin(u - sin u) ~ u^3/3); the next line ('3 log(x_{2n-1}) + O(1)') is consistent with the corrected version.
  - **Proposal:** Line 42: '$x_{2n+1} = \dfrac{x_{2n-1}}{3} + o(x_{2n-1}^3)$' should read '$x_{2n+1} = \dfrac{x_{2n-1}^{3}}{3} + o(x_{2n-1}^3)$' (from $u-\sin u+\sin(u-\sin u)\sim u^3/3$); the next line's '$3\log(x_{2n-1})+O(1)$' already matches the corrected cubic version.
- **0044** — Part 1: the question asks for the RANGE of x_0 for which the limit exists; the solution shows convergence to 0 (for every x_0, via \|x_1\| <= 1) but never states the answer explicitly and ends with 'the argument ... is now easy to establish'.
  - **Proposal:** Part 1 never states its answer (the range of $x_0$ for which the limit exists, and its value). Suggest adding after 'the argument that $x_n$ tends to 0 is now easy to establish' (end of Part 1, line 38): 'Hence the limit exists for every $x_0\in\mathbb{R}$, and $\lim_{n\to\infty}x_n=0$.'
- **0044** — The recursion x_{n+1} = x_n + (-1)^n sin(x_n) is stated 'for n >= 2' but holds for n >= 1.
  - **Proposal:** Line 24: '$x_{n+1} = x_n + (-1)^n \sin(x_n), \quad n \geq 2$' should read '$n\geq 1$' — the telescoping identity already holds from $n=1$ (e.g. $x_2=x_1-\sin(x_1)$ matches $x_2=\sin(x_0)-\sin(x_1)$ since $x_1=\sin(x_0)$).
- **0045** — Solution steps 'a = a^{m-(m-1)} = a^{n-(m-1)}' and 'g^{r-(r-1)} = g^{s-(r-1)}' silently cancel a^{m-1} (resp. g^{r-1}) using the injectivity hypothesis; when m = 1 (resp. r = 1) the exponent m-1 = 0 is not defined by the statement (the case is trivially fine since then a = a^n directly).
  - **Proposal:** Add an explicit case split at m=1 (resp. r=1). The displayed chain 'a=a^1=a^{m-(m-1)}=a^{n-(m-1)}=a\cdot a^{n-m}=a\cdot e_S' implicitly cancels a well-defined a^{m-1} on the left (using left-injectivity), which needs m-1\ge1, i.e. m\ge2. When m=1, a^{m-1}=a^0 is not defined by the statement. Fix: treat m=1 separately -- then a=a^1=a^n directly (the hypothesis with no shift), so with e_S:=a^{n-1} (n-1\ge1 since n>m=1) we get a\cdot e_S=a\cdot a^{n-1}=a^n=a straight from the recursive definition of the exponent, with no cancellation needed. State 'for m\ge2, ...' before the displayed chain, and add the m=1 case as above. Same fix, symmetrically, for 'g^{r-(r-1)}=g^{s-(r-1)}' when r=1.
- **0046** — Solution 2, rational case: 'ran(x') subset N' is wrong (x' = lcm(q_i).x may have negative entries); only x'' = x' - min ran(x') has range in N. Harmless for the argument.
  - Typo part already fixed: '\subset \operatorname{ran}(\mathbf{x}') \subset \mathbb{N}' -> '\subset \mathbb{Z}' (x' = lcm(q_i)\cdot x can have negative entries; only x''=x'-\min\operatorname{ran}(x') is shown to land in \mathbb{N} later, unaffected).
  - **Proposal:** Already fixed in the current file: the line reads '... \subset \operatorname{ran}(\mathbf{x}') \subset \mathbb{Z}' (not N) — no action needed.
- **0049** — Bonus asks for g in {cos, sin}; the solution only handles cos. sin is never addressed.
  - **Proposal:** The bonus's sin case has no argument at all. sin's only fixed point is 0, with sin'(0)=1>0, unlike cos's Dottie number \alpha where cos'(\alpha)=-\sin(\alpha)<0; so the non-existence argument used for cos (the lemma requiring local strict decrease at the fixed point) does not transfer -- sin is strictly increasing near 0, not decreasing. Someone needs to work out whether a continuous g with g\circ g=\sin exists at all (the parabolic, non-hyperbolic fixed point makes this a genuinely different case) and write the corresponding proof; this is not a small edit.
- **0049** — A \begin{lemma} block contains the text 'Spas ? it would be nice if you could make a lemma where such a g exists ...' (a note to a co-author, not a lemma) -- placeholder to remove or replace.
  - **Proposal:** Delete the stray \begin{lemma}...\end{lemma} block reading 'Spas ? it would be nice if you could make a lemma where such a g exists ...' -- it is a note to a co-author, not mathematical content. The lemma it asks for already exists right after it (the 'iterative square root' Lemma for strictly increasing f). If that later lemma is judged sufficient (see E91 first), simply remove this block; otherwise replace it with a precise statement of what is still missing.
- **0049** — Iterative-square-root lemma (strictly increasing f): the hypothesis 'f(x) > x or f(x) < x between adjacent fixed points' is automatic by the IVT; the construction uses f^{-1} and a two-sided orbit (x_n, n in Z), which needs f to map (a,b) onto itself -- not guaranteed when an endpoint is +/- infinity and f is not surjective (e.g. f(x) = -exp(-x)); the backward extension (n < 0) is not written and continuity at the fixed points is only asserted.
  - **Proposal:** Replace: 'Let $f: \mathbb{R} \to \mathbb{R}$ be a strictly increasing continuous function. Suppose that on any interval $(a, b)$ where $a$ and $b$ are adjacent fixed points (i.e., $f(a)=a, f(b)=b$ and $f(x) \neq x$ for $x \in (a, b)$), the function satisfies either $f(x) > x$ or $f(x) < x$. Then there exists a continuous strictly increasing function $g$ such that $g\circ g=f$.' With: '... Suppose that on any such interval $(a,b)$, $f$ maps $(a,b)$ onto itself. (The condition 'f(x)>x or f(x)<x throughout' is automatic by the IVT and continuity, so it is not an extra hypothesis; what the construction actually needs is that $f((a,b))=(a,b)$, which fails e.g. for $f(x)=-e^{-x}$ on $(-\infty,0)$.) Then there exists ...' Also, in the proof: (1) define $x_n$ for $n<0$ explicitly, e.g. $x_{n-1}:=f^{-1}(x_n)$, using $f((a,b))=(a,b)$ to keep every $x_n\in(a,b)$; (2) replace 'the monotonic nature of the construction ensures continuity at the fixed points' by an actual argument, e.g. $(x_n)_{n<0}$ is monotone and bounded below by $a$ so $x_n\to a$, $g$ is monotone on each $[x_n,x_{n+1}]$, hence $g(x_n)\to g(a)=a$ too, giving continuity of $g$ at $a$ (symmetrically at $b$).
- **0049** — Typography in the cos solution: cos, sin, lim, max, min, cte are typeset without backslash ($cos$, $sin$, ...); 'metrix space', 'tell us', 'unicity' (uniqueness). Sentence '(and the surjectivity of g as well over [0;1])' is loosely stated (g maps g([0,pi/2]) onto [0,1]).
  - Typo part already fixed: Backslash restored throughout the cos-bonus block: cos, sin, lim, max, min -> \cos, \sin, \lim, \max, \min (all occurrences), and 'cte_{\alpha}' -> '\mathrm{cte}_{\alpha}'; 'metrix space' -> 'metric space'; 'unicity' -> 'uniqueness' (2 occurrences); 'tell us' -> 'tells us' (2 occurrences).
  - **Proposal:** The parenthetical '(and the surjectivity of g as well over [0;1])' overclaims: what is actually justified at that point is only that $g\circ g$ maps $[0,\pi/2]$ onto $\cos([0,\pi/2])=[0,1]$, not that $g$ itself is onto $[0,1]$. Note: on re-reading the file this phrase now reads '(that is, $g$ maps $[0;\pi/2]$ onto $[0;1]$)' -- it looks like this was already edited on disk independently of this pass; if so it should probably still say '$g\circ g$ maps ... onto [0,1]' rather than '$g$ maps ... onto [0,1]', to only claim what the argument actually proves.
- **0050** — Solution: 'For m != 1, \|mn\| >= 2\|n\|' needs \|m\| >= 2 (m = -1 is not excluded); the case m = 1 stops mid-sentence ('\|1 - n^2\| >= 3 and then .'). Moreover the claimed monovariant d_{k+1} >= 2 d_k is FALSE when \|m\| = 1: f(1/n) = (1-n^2)/n keeps the denominator \|n\|. The correct statement is: the denominator never decreases and strictly increases at least every two steps (after \|m\| = 1 the new numerator has absolute value >= 3), which still gives the conclusion; the 2^k bound in the last paragraph should be adapted accordingly.
  - **Proposal:** Two fixes needed. (1) 'For $m\neq1$, $|mn|\ge2|n|$' should read '$|m|\ge2$' (as written it does not exclude $m=-1$). (2) The $m=1$ sentence is cut off ('$|1-n^2|\ge3$ and then .'), and the claimed monovariant $d_{k+1}\ge2d_k$ is false when $|m|=1$: $f(1/n)=(1-n^2)/n$ keeps denominator $|n|$. Replace the growth paragraph with: the denominator (in lowest terms) never decreases, and at least doubles within any two consecutive steps (because a step with $|m|=1$ forces the very next numerator to have absolute value $\ge3$, hence $|m|\ge2$ and genuine doubling at that next step); conclude $d_k\ge2^{\lfloor k/2\rfloor}d_0$, and adjust the final '$2^k$' bound in the last paragraph to this weaker (but still $\to\infty$) rate.
- **0051** — Solution l. 380: reservoir sampling is introduced as selecting 'k >= 1 items without replacement'; only the k = 1 case is presented and proved.
  - **Proposal:** The intro sentence claims the general family samples 'of $k \geq 1$ items ... from a population of unknown size $n \geq k$', but only $k=1$ is proved. Minimal fix (narrowing the claim to match the proof): replace 'of $k \geq 1$ items from a population of unknown size $n\geq k$' with 'of a single item ($k=1$) from a population of unknown size $n\geq1$'. (Keeping the general-$k$ claim instead would require adding a full proof for $k>1$, a separate and larger piece of work.)
- **0052** — Statement: 'domain containing the disc D; that is, D \subset U' -- f is evaluated on \partial D, so the CLOSED disc (closure of D) should be contained in U.
  - **Proposal:** Replace: 'Let \( U \subset \mathbb{C} \) be a domain containing the disc \( \mathbb{D} \); that is, \( \mathbb{D} \subset U \)' with: 'Let \( U \subset \mathbb{C} \) be a domain containing the closed disc \( \overline{\mathbb{D}} \); that is, \( \overline{\mathbb{D}} \subset U \)'. Reason: $f$ is evaluated on $\partial\mathbb{D}\subset\overline{\mathbb{D}}$, which need not lie in $U$ if only the open disc $\mathbb{D}\subset U$ is assumed.
- **0053** — The solution does not say which parity of n gives zeros and which gives nines ((5 - sqrt26)^n is negative for odd n, giving the nines); the statement only asks for 'either', so this is not a gap.
  - **Proposal:** No fix needed — the entry itself notes this is not a gap: the statement only asks to show the decimal part begins with either n zeros or n nines, not which parity gives which, so the solution's silence on this is not an error.
- **0055** — Riddles 4: arithmetic error (maintainer)
  - **Proposal:** Section header grouping E104-E106 under 'Riddles 4' (prisoners and the light bulb); no separate action, see E104-E106.
- **0055** — Solution l. 468-471 (the known arithmetic error): '100 * H_99 ~ 100 * ln(99) + gamma ~ 5180 days' is wrong three times over: 100*H_99 ~ 518 (not 5180); gamma should be inside the bracket, 100*(ln 99 + gamma); and 100*H_99 is the coupon-collector time until every prisoner has visited once, not the expected duration of the single-counter strategy, which is 100*H_99 + 99*100 ~ 10 418 days (about 28.5 years) for 100 prisoners.
  - **Proposal:** Solution's displayed formula '100 \cdot H_{99} \approx 100 \cdot \ln(99) + \gamma \approx 5180 \text{ days}' has 3 errors. Replace with: 100 H_99 ~ 100(ln 99 + gamma) ~ 519 days is only the expected time for the coupon-collector phase (every prisoner visited once); the counter-strategy's actual expected release time is this plus the additional wait for the counter to register all 99 toggles afterward, i.e. 100 H_99 + 99*100 ~ 10,418 days (about 28.5 years). Suggested replacement text: '100 \cdot H_{99} \approx 100 \cdot(\ln 99+\gamma)\approx 519 \text{ days}' is the expected coupon-collector time (every prisoner has visited once); the counter-based strategy's expected release time is 100\cdot H_{99}+99\cdot100\approx 10\,418 \text{ days (about 28.5 years)}.
- **0055** — Solution l. 466: 'with certainty' means almost surely (probability 1); no strategy can guarantee success by a fixed day, so 'eventual success with certainty' in the statement should be read as 'with probability 1'.
  - **Proposal:** Statement l.21 'ensure their eventual release, with certainty?' and solution's closing sentence 'the strategy will eventually succeed with certainty' overstate the guarantee: with a uniformly random daily visitor, no strategy guarantees a release by any fixed day; success is only almost sure (probability 1), since the warden could (with vanishing but nonzero probability) never call some prisoner for arbitrarily long. Suggested fix: replace 'with certainty' (statement) by 'almost surely (i.e. with probability 1)' and 'will eventually succeed with certainty' (solution) by 'will succeed with probability 1'.
- **0055** — Statement l. 57: 'the best-known strategies achieve an average release time of approximately 3500 days' is given without a source.
  - **Proposal:** Statement l.27 'The best-known strategies achieve an average release time of approximately 3500 days' is uncited in the statement itself; the file's own header % references field already sources this figure (Wu's Note 1). Proposal: either drop the sentence from the statement (it is already flagged as informational) or move the ~3500-day figure and its citation into the solution's remark, next to the reference already on file; no numeric value needs changing.
- **0056** — Solution (a), l. 484: the argument only shows probability ~ 1/4 for large N via '(almost) independent' events; it does not prove the required guarantee >= 1/4. The exact success probability of the half/half strategy is N/(4(N-1)) > 1/4.
  - **Proposal:** Part (a)'s argument only shows the ~1/4 probability heuristically ('(presque) indépendants', N large); it doesn't establish the required guarantee >= 1/4 for all N. Add: the exact success probability of the half/half strategy is N/(4(N-1)) (for N even splitting into two halves of N/2), which is >= 1/4 for every N >= 2, giving the guarantee rigorously without the large-N approximation.
- **0056** — Solution (b), l. 501: 'sommer sur tous les p>n possibles' should read p > x (the sum runs over p = x+1, ..., n).
  - **Proposal:** Line 'il faut sommer sur tous les $p>n$ possibles' should read 'il faut sommer sur tous les $p>x$ possibles' (matches the sum $\sum_{p=x+1}^n$ actually written just below); replace $p>n$ by $p>x$.
- **0056** — Solution (b) switches from N (statement) to n in all formulas.
  - **Proposal:** Part (b) uses N in the statement's setup line but switches to n throughout all subsequent formulas (steps ii-vi) without saying n:=N or introducing n as the total number of suitors. Add a line right after 'Stratégie: ...' introducing n := N (or replace every n in part (b) by N) so the variable is consistent with the statement.
- **0056** — Solution (b) optimises only within the family of threshold strategies (reject the first x, then take the first better one) and uses the approximation sum 1/p ~ log; optimality of this family among all strategies is asserted, not proved.
  - **Proposal:** Step vi only optimizes -alpha*log(alpha) within the family of threshold strategies (reject the first x, then take the first candidate better than all seen so far) and uses the harmonic-sum approximation log(n/x); it doesn't establish that no other (non-threshold) strategy does better. A full optimality proof requires an argument (e.g. backward induction / optimal stopping) that threshold strategies are optimal among all strategies; this is not attempted here and would need to be added, or the claim 'the optimal strategy' softened to 'the optimal threshold strategy'.
- **0056** — Solution l. 516-520: author's note 'Penser à peut etre faire une version du prophet inequality problem' and three raw URLs left inside the solution.
  - **Proposal:** The author's leftover note 'Penser à peut-être faire une version du prophet inequality problem' and the three raw \url{...} lines at the end of the solution are scratch/reference material, not part of the proof. Proposal: delete that sentence and the three \url{} lines (keep them elsewhere, e.g. in the % references header, if the maintainer wants to keep the prophet-inequality lead).
- **0057** — Solution line 1081: 'positive derivative \|alpha\| x^{\|alpha\|}' -- the derivative of x^{\|alpha\|} is \|alpha\| x^{\|alpha\|-1}.
  - **Proposal:** Replace 'positive derivative $|\alpha|\cdot x^{|\alpha|}$' by 'positive derivative $|\alpha|\cdot x^{|\alpha|-1}$' (derivative of $x^{|\alpha|}$ is $|\alpha| x^{|\alpha|-1}$, not $|\alpha| x^{|\alpha|}$).
- **0057** — Solution line 1103: the third-order difference is written (n+3)^a-3(n+2)^a+(n+1)^a-n^a; the coefficient of (n+1)^a should be +3; it is also claimed to lie in N^* whereas only membership in Z is justified.
  - **Proposal:** The 3rd-order difference '(n+3)^a-3(n+2)^a+(n+1)^a-n^a' has the wrong coefficient on the (n+1)^a term: the correct 3rd finite difference is (n+3)^a - 3(n+2)^a + 3(n+1)^a - n^a (binomial coefficients 1,-3,3,-1). Replace '+(n+1)^{\alpha}' by '+3(n+1)^{\alpha}' in that displayed line. Also, membership is only justified in Z there (as a difference of integers, if (n+k)^alpha in N for all k), not in N^*; either justify positivity separately or state membership in Z only at that point.
- **0057** — Solution line 1109: the coefficient is written prod_{i=0}^{floor(a)+1}(a-i) with exponent (a-(floor(a)+1)); the (floor(a)+1)-th derivative of x^a has coefficient prod_{i=0}^{floor(a)}(a-i) (as used correctly on line 1164).
  - **Proposal:** The coefficient/exponent 'which really is $\prod_{i=0}^{\lfloor\alpha\rfloor+1}(\alpha-i)$ multiplied by $(n+\theta_{\lfloor\alpha\rfloor+1})^{(\alpha-(\lfloor\alpha\rfloor+1))<0}$' has the wrong upper index on the product: it should be $\prod_{i=0}^{\lfloor\alpha\rfloor}(\alpha-i)$ (i.e. stop at $\lfloor\alpha\rfloor$, not $\lfloor\alpha\rfloor+1$), matching the correct usage later in the file (product $\prod_{i=0}^{\lfloor\alpha\rfloor}(\alpha-i)$, used e.g. around the closed-form derivative computation near the end of the solution).
- **0057** — Solution lines 1157-1162: f(x)=x^alpha on R_+, 0 elsewhere is claimed to be C^infinity(R,R); this is false for non-integer alpha (derivatives of order > alpha blow up at 0). The argument is repairable by working on ]0,+infty[ since all evaluation points n+theta are positive, but as written the lemma is applied outside its hypotheses.
  - **Proposal:** The claim 'we have $f\in C^{\infty}(\mathbb{R},\mathbb{R})$' for f(x) = x^alpha on R_+ and 0 elsewhere is false for non-integer alpha (derivatives of order > alpha blow up as x -> 0+, and f isn't even continuous at 0 in general). Fix: restate the smoothness/finite-difference lemma (the set A = N result) as applying on the open interval ]0, +infty[ instead of all of R, and note all evaluation points used (n + theta, n in N*) are already positive, so the restriction costs nothing; drop the claim f in C^infinity(R,R) and instead take f(x) = x^alpha directly on ]0,+infty[.
- **0059** — NT8: perfect-power gap in the solution (maintainer)
  - **Proposal:** Header entry grouping E119-E120 under 'NT8: perfect-power gap in the solution'; no separate action, see E119.
- **0059** — Perfect-power gap (confirmed): the bound {m^(1/n)} >= 1/(nm) is derived from '1 <= m - a^n', which fails when m is a perfect n-th power (then the fractional part is 0; this includes n = 1 and m = 1). The cases where m^(1/n) or n^(1/m) is an integer are not treated, although they are easy (the LHS is then either an integer >= 2 while 2 < RHS < 3, or at least 3 > RHS).
  - **Proposal:** The bound '{sqrt[n]{m}} >= 1/(nm)' is derived from '1 <= m - a^n', which fails when m = a^n exactly (fractional part 0), including n=1 or m=1. Add a separate case: if sqrt[n]{m} (or sqrt[m]{n}) is itself an integer, then either it is an integer >= 2, giving LHS = sqrt[n]{m}+sqrt[m]{n} >= 2 + (something >=1) hence LHS >= 3 while the RHS is < 3 (since 2 < RHS < 3 as m,n>=1), or the other radical forces LHS >= 3 directly; in either subcase LHS > RHS still holds, so no solution arises there either. This case split should be added before invoking the general bound 1/(nm).
- **0062** — The claim 'irrational number but not a transcendental number' looks false as stated: the partial sums are rationals p/q with q = 9^(F_{n+2}) (powers of 3) and error about q^(-F_{n+3}/F_{n+2}) ~ q^(-1.618), so by Ridout's theorem (p-adic Roth) the sum is transcendental. The original problem presumably asks only for irrationality (or reads differently); to be checked against the source before anyone writes a solution.
  - **Proposal:** The statement's closing claim '... but not a transcendental number' looks false, and the file's own reference already flags it: the Pentagon editor's note says the 'not transcendental' part was never proved and remains open. Independently, since the partial sums are rationals p/9^{F_{n+2}} with error ~ 9^{-F_{n+2}} * 9^{-(F_{n+3}-F_{n+2})} decaying like q^{-1.618} in the denominator q=9^{F_{n+2}}, Ridout's theorem (p-adic Roth) would actually make the sum transcendental, contradicting the statement. Recommendation: do not attempt a solution proving 'not transcendental' as currently stated; check the original Pentagon problem wording (it may only claim irrationality) and adjust the statement to match the source, e.g. drop 'but not a transcendental number' or replace with 'is transcendental' pending verification -- needs the source problem re-checked, not an invented fix.
- **0063** — NT10: the example f(x) = 3x^2 + x + 1 is wrong (maintainer)
  - **Proposal:** Header entry: 'the example f(x)=3x^2+x+1 is wrong'; no separate action, see E124.
- **0063** — Known and confirmed: the example f(x) = 3x^2 + x + 1 does NOT work; brute force shows its orbit of 0 reaches only 24 residues mod 225 (3 residues mod 9, 8 mod 25). A brute-force search shows that suitable (a, b, c) do exist, so the intended answer 'yes' stands, but the example and the final verification must be redone.
  - **Proposal:** Confirmed by brute force: f(x)=3x^2+x+1's orbit of 0 mod 225 has only 24 elements (3 residues mod 9, 8 mod 25), so it does not cover all 225 remainders; the example and the final verification sentence ('This is indeed the case, as one can check: to finish') must be redone with a working (a,b,c). Verified working replacements (brute-forced, orbit of 0 hits all 225 residues mod 225): the paper's own answer a=150, b=1, c=1 works, and so does the smaller a=15, b=1, c=1. Recommend replacing 'a = 3 and b = 1, so that our function is f(x) = 3x^2+x+1' with a verified pair such as a=15, b=1 (f(x)=15x^2+x+1) or the source's a=150, b=1 (f(x)=150x^2+x+1), together with a mod-9 and mod-25 orbit check finishing the proof (currently just '\textcolor{red}{to finish}').
- **0063** — The reduction to c = 1 is misstated: conjugating by z -> cz turns (a, b, c) into (ca, b, 1), not into (c^{-1} a, c^{-1} b, 1) as written (f = c g does not make the orbits of f and g correspond).
  - **Proposal:** The reduction step is misstated. Conjugating f(z)=az^2+bz+c by z -> cz gives g(w) = c^{-1}f(cw) = ac*w^2 + b*w + 1, i.e. (a,b,c) reduces to (a'=ca, b'=b, c'=1) -- NOT (a'=c^{-1}a, b'=c^{-1}b, c'=1) as currently written. Replace '[a']_{225} = [c]_{225}^{-1} \cdot [a]_{225}$ and $[b']_{225} = [c]_{225}^{-1} \cdot [b]_{225}' with '[a']_{225} = [c]_{225} \cdot [a]_{225}$ and $[b']_{225} = [b]_{225}' (b is unchanged by the conjugation).
- **0063** — The CRT step ('it suffices to cover all residues mod 9 and mod 25') silently uses that the orbits mod 9 and mod 25 are pure cycles of lengths 9 and 25 (so indices run over all pairs by CRT); this is true when f permutes the residues but is not spelled out.
  - **Proposal:** The CRT step ('it suffices to show that the sequence leaves all remainders when divided by 9 and by 25') silently assumes the orbit mod 9 (resp. mod 25) is a single cycle of full length 9 (resp. 25), so that by CRT every pair of residues is eventually hit simultaneously; if instead the two component sequences merely visited all residues at possibly different, unsynchronized times, hitting every pair mod 225 wouldn't automatically follow. Add a sentence justifying that f induces a permutation (single full cycle) of Z/9Z and of Z/25Z under the chosen a,b,c, before invoking CRT to combine them.
- **0063** — Uses \textcolor{red}{...} and \href (hyperref) in the solution body; compiles with the bank preamble.
  - **Proposal:** Informational only: the solution uses \textcolor{red}{...} and \href (hyperref) in its body; this compiles fine with the bank preamble, no fix needed. (The \textcolor{red}{to finish} placeholder itself just marks the unfinished proof addressed by E124/E126.)
- **0069** — Solution unfinished: it ends with 'the first term tends to, the second to, the third one to. So we sum and obtain:' with no values and no final answer.
  - **Proposal:** The solution stops before the answer. Complete it: with a=⌊√n⌋, b=⌊√(2n)⌋, the three additive terms of S/n^{3/2} (i) [a(a+1)(2a+1)-6a+6(2n+1)(b-a)]/(6n^{3/2}) -> 2√2-5/3, (ii) -b(b+1)(2b+1)/(6n^{3/2}) -> -2√2/3, (iii) a(a+1)(2a+1)/(6n^{3/2}) -> 1/3. Sum = 4(√2-1)/3, i.e. append: "the first term tends to $2\sqrt2-\frac53$, the second to $-\frac{2\sqrt2}{3}$, the third one to $\frac13$. So we sum and obtain: $\lim_{n\to\infty}\sqrt{n}\,p_n=\dfrac{4(\sqrt2-1)}{3}$." (matches the known Putnam 1982 B3 answer).
- **0069** — Typos: 'soltuions'; the letter n is reused for a generic integer ('writing n as the sum of two') while n is already the problem's parameter; 'writing n' has n outside math mode.
  - Typo part already fixed: "soltuions" -> "solutions"; "writing n" -> "writing $n$" (was outside math mode)
  - **Proposal:** The generic dummy variable also called n in the opening sentence ('for each $n\in\mathbb N^{*}$ the number of ways of writing $n$ as the sum of two $a,b\in\mathbb N^{*}$ is $n-1$ ... between $1$ and $n-1$ ... i.e. $n-1$') collides with the problem's own parameter n used everywhere after. Rename it throughout that one sentence, e.g. to k: 'for each $k\in\mathbb{N}^{*}$ the number of ways of writing $k$ as the sum of two $a,b\in\mathbb{N}^{*}$ is $k-1$ (indeed fix $a$ it must be between $1$ and $k-1$ for $b$ to be determined so there are as many solutions as there are as many $a$ i.e. $k-1$)'.
- **0070** — Statement (problems version) does not exclude the zero polynomial, whereas the solution (l. 532, 537) assumes P non-zero (harmless: P(1) = 0 already forces P = 0).
  - **Proposal:** Add to the problem statement the non-zero hypothesis the solution already assumes ($P\in\mathbb N[X]\setminus\{0\}$): append to "You do know that the coefficients are all non-negative integers." the clause "and that the polynomial is not identically zero."
- **0070** — Statement Bonus says 'real number mode' (input a real number); the solution answers a reformulated 'computable mode' (input an algorithm computing a real r, receive an algorithm computing P(r)).
  - **Proposal:** Replace the Bonus statement "you can switch the machine to real number mode, where you can input a real number" by the computable-mode formulation the solution actually answers: "you can switch the machine to computable mode, where you input a deterministic algorithm that computes a real number to any requested precision, and the machine returns an algorithm that computes $P$ of that number to any requested precision."
- **0070** — Bonus solution l. 615: the bound 'n <= T-1', with T the first integer such that 3^T > q, is wrong — it uses e < 3, which is the wrong direction (e^n < q < 3^T only gives n < T ln 3). Counter-example: P = X, s = e, q = 2.9 gives T = 1 but n = 1. Powers of 2 would work (2 < e, so 2^n < q < 2^T gives n <= T-1).
  - **Proposal:** The powers-of-3 bound uses the wrong inequality direction. Replace it by powers of 2 (already the alternative used two lines later for $a_i$): "simply compute recursively $2^0,2^1,\dots,2^k,\dots$ until finding the first $T\in\mathbb N$ that satisfies $2^T>q$. Because $2<e$, we get $2^n\le e^n\le a_ne^n\le P(e)=s<q<2^T$, so $2^n<2^T$, hence $0\le n\le T-1$." (counter-example given for the original: P=X, s=e, q=2.9 gives T=1 with the true n=1 already violating n<=T-1).
- **0070** — Bonus solution l. 623: 'we can certifies' (grammar); comparing 'decimal places' of s and Q(e) should rather be comparing rational approximations (digits of a computable real are not uniformly computable from an approximation algorithm; here it is fine since Q(e) is irrational for non-constant Q).
  - Typo part already fixed: "we can certifies this inequality" -> "we can certify this inequality"
  - **Proposal:** Replace the 'decimal place' argument by rational approximations, which is what is actually computable: "Thus $s\neq Q(e)$, so there is $\varepsilon>0$ with $|s-Q(e)|>\varepsilon$; computing rational approximations of $s$ and of $Q(e)$ to precision better than $\varepsilon/2$ certifies $s\neq Q(e)$ in finitely many steps, allowing $a'$ to be discarded." (comparing raw decimal digits is not legitimate in general, since digits of a computable real need not be uniformly computable from an approximation algorithm; harmless here only because $Q(e)$ is irrational for non-constant $Q$).
- **0075** — Statement says 'for every integer n'; it should read 'for every positive integer n' (the set {a+1, ..., a+n} and floor(n/4) make no sense for n <= 0).
  - **Proposal:** "Prove that for every integer $n$" -> "Prove that for every positive integer $n$" (the set $\{a+1,\ldots,a+n\}$ and $\lfloor n/4\rfloor$ require $n\ge1$).
- **0080** — Typos in the statement: 'subsets with for elements' (four); 'Fix an integer m' should be a positive integer.
  - Typo part already fixed: "subsets with for elements" -> "subsets with four elements"
  - **Proposal:** "Fix an integer $m$" -> "Fix a positive integer $m$" (the answer $n=5^m$ / the Steiner-system construction needs $m\ge1$).

## Found during the history pass

- **0062** — the "not transcendental" claim is likely false; *The Pentagon*'s own editor note (2024) says that part is unproved. Consider asking only for irrationality.
  - **Proposal:** Duplicate of E121: same 'not a transcendental number' concern (partial sums are rationals p/9^{F_{n+2}} with error ~ q^{-1.618}, so by Ridout's theorem the series should be transcendental, not merely irrational-but-not-transcendental as stated). See E121's proposal; needs the original source checked before any fix to the statement.
- **0080** — m is a natural number in the original; typo "for elements".
  - Typo part already fixed: same fix as E142: "for elements" -> "four elements"
  - **Proposal:** m should be stated as a positive integer, not just an integer (same point as E142).
- **0052** — the statement only assumes the closed disc in U.
  - **Proposal:** Same fix as E99: the hypothesis should be stated as $\overline{\mathbb{D}}\subset U$ (closed disc), not just $\mathbb{D}\subset U$; see E99's proposed replacement text.
- **0021, 0024**: the club statement is the transpose of the official one (equivalent).
- **0069**: solution unfinished (limits left blank).

## Missing and partial solutions

- no solution: 0052, 0060, 0061, 0062, 0065, 0066, 0067, 0068, 0071, 0072, 0073, 0074, 0075, 0076, 0077, 0078, 0079, 0080
- partial: 0019, 0031, 0034, 0038, 0039, 0040, 0049, 0063, 0064, 0069
